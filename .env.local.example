# Frontend Environment Variables (Local Development)

# Backend API URL (local development)
NEXT_PUBLIC_API_URL=http://localhost:3001

# WebSocket URL (local development)
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# Production URLs are configured in src/config/api.ts
# No environment variables needed for production deployment
# Vercel will use the hardcoded production URLs from the config file
